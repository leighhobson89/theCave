# Notifications

Toast notifications as shortcuts to the desk object they are about.

| Spec | Covers |
| --- | --- |
| `window-shortcuts.spec.js` | Routing to all three targets, closing the computer first, dismissal, the already-open case, keyboard access, never covering a window's close button |

Not one of the originally scoped 10 categories — added because these tests
didn't fit any of them and folding them into an unrelated folder would have
made that folder's name inaccurate.
